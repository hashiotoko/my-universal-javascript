scalar DateTime

type Team {
  id: ID!
  name: String!
  postalCode: String!
  mailAddress: String!
  firstLoggedInAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Query {
  teams: [Team]!
}

schema {
  query: Query
}
